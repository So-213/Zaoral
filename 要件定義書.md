# ザオラルサイト – 要件定義書（v1）

## 0. 概要
- **目的**  
  マッチングアプリで連絡が途切れた相手に対し、URLで返信を促す “軽いLP” を超簡単に作って送れるSaaS。
- **提供価値**  
  - 既読 / 反応促進
  - URLごとの個別メッセージ作成

## 1. 対象ユーザー / ペルソナ
- マッチングアプリを利用し、複数の連絡先を並行管理しているユーザー

## 2. ユースケース（MVP）
| ID | ユースケース |
|----|--------------|
| U1 | Google / LINE でログインしたい |
| U2 | メッセージ入力で URL を発行したい |
| U3 | 作成した URL を一覧でコピーして送付したい |



## 3. スコープ（MVP）
1. Google / LINE ログイン（Auth.js）
2. 「メッセージ型」プロジェクトの作成
3. 公開ページ：`/u/:slug`
4. ダッシュボード：自分のプロジェクト一覧

### 将来の拡張候補
- ページタイプ追加
- トライアル / 課金（Stripe）
- In-app browser 検知 → 外部ブラウザ誘導（403対策）※未実装



## 4. 機能要件

### 4.1 認証 / 認可
- Auth.js
- JWT ベース
- Google / LINE ログイン対応

### 4.2 プロジェクト（type = `message`）
- **生成**
  - type = `message`
  - `slug` 自動生成
- **属性**
  - `slug`
  - `expires_at`
  - `message`（子テーブル）
- **一覧**
  - ログインユーザーの `projects` を作成順で取得
- **公開ページ**
  - `/u/:slug`  
  - 期限内のみアクセス可能

### 4.3 ブラウザポリシー対策
- In-app browser を検知
- `/open-in-browser` へ誘導


