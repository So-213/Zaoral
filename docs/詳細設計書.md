# 詳細設計書（v1）

## 1. 技術スタック

### フロントエンド
- **Next.js 14** 
- **TypeScript** 
- **Tailwind CSS** 
- **NextAuth.js v5** 

### バックエンド
- **Next.js 14など** (Zaoral_backend3参照)
- **PostgreSQL** 
- **Prisma** 

## 2. 画面/フロー

- `/` → ホーム
- `/zaoral` → アプリ説明ページ
- `/howToUse` → 使い方説明ページ
- `/dashboard` → 新規作成ボタン・プロジェクト一覧・詳細表示・URLコピー機能
- `/create` → 入力→作成→URL表示
- `/auth/signin` → Google/LINEログイン開始
- `/account` → アカウント設定ページ
- `/p/:slug` → 公開ページ（Zaoral_backen3参照）

## 3. API（MVP）

### 認証
- `GET/POST /api/auth/[...nextauth]`
  - 機能: NextAuth認証ハンドラ
  - 認証方法: Google / LINE OAuth

### プロジェクト管理
- `POST /api/projects`
  - **認証**: 必須
  - **リクエスト**: 
    ```json
    {
      "slug": "string (6文字のランダム文字列、フロントエンドで生成)",
      "message": "string (必須、50文字以内)"
    }
    ```
  - **レスポンス**: `201 Created`
    ```json
    {
      "id": "string",
      "slug": "string",
      "user_id": "string",
      "user_name": "string",
      "type": "message",
      "created_at": "DateTime",
      "expires_at": "DateTime (作成日時+31日)",
      "published": false,
      "projectMessage": {
        "message": "string"
      }
    }
    ```
  - **機能**: プロジェクト作成（有効期限は自動で31日後に設定）
  - **バリデーション**: slugとmessageは必須
  
- `GET /api/projects`
  - **認証**: 必須
  - **レスポンス**: `200 OK`
    ```json
    {
      "projects": [
        {
          "id": "string",
          "slug": "string",
          "created_at": "DateTime",
          "expires_at": "DateTime",
          "published": boolean,
          "projectMessage": {
            "message": "string"
          }
        }
      ]
    }
    ```
  - **機能**: ログインユーザーのプロジェクト一覧取得
  - **フィルタ**: 有効期限切れ1ヶ月後以内のプロジェクトのみ
  
- `DELETE /api/projects?id=:id`
  - **認証**: 必須
  - **クエリパラメータ**: `id` (プロジェクトID)
  - **レスポンス**: `200 OK`
    ```json
    {
      "message": "プロジェクトが正常に削除されました"
    }
    ```
  - **機能**: プロジェクト削除
  - **権限チェック**: 所有者のみ削除可能
  - **カスケード削除**: 関連するProjectMessageも自動削除

### 公開機能（Zaoral_backend3）
- `GET /p/:slug`
  - **認証**: 不要（公開エンドポイント）
  - **機能**: 公開プロジェクト取得（有効期限内のみ）
  - **詳細**: 別バックエンドサービス（Zaoral_backend3）にて実装

## 4. ER図（PostgreSQL）（MVP）
<img src="../public/ER.png" alt="ER図" width="70%" />
