# 01. 調査・現状把握

- 実施日: 2026-08-11
- 対象: `Zaoral`リポジトリ本体(`Zaoral_backend2`, `Zaoral_backend3`は対象外)

## やったこと

フロント/バックエンド分離の計画を立てる前提として、現行プロジェクトの以下を調査した。

1. 認証・認可の実装方式
2. DB関連の実装状況(スキーマ・service層分離度合い)
3. AWS関連の実装状況
4. API構成(エンドポイント数・service層構成)
5. デプロイ/インフラ構成
6. 主要依存パッケージ

## わかったこと

### プロジェクト概要
- マッチングアプリなどで返信が途切れた相手に返信を促す軽いLPを作れるSaaS(`docs/要件定義書.md`)。
- MVP機能: Google/LINE認証、メッセージ/画像(picture)型プロジェクト作成、ダッシュボード管理、公開ページ。

### 認証・認可
- **NextAuth v5(beta)** + `@auth/prisma-adapter`。設定は `src/lib/auth.ts`。
- プロバイダー: Google, LINE, 開発用credentialsプロバイダー。
- セッション戦略は **JWT**(PrismaAdapterでAccount/Session等のテーブルは用意されているが、実運用はJWT)。
- ルート保護は各API routeの `requireAuth()`(`src/lib/api-helpers.ts`)。認可はロールベースではなく所有者チェックのみ(`checkProjectOwnership`, `src/lib/services/project-service.ts`)。

### DB関連
- `prisma/schema.prisma`: PostgreSQL、9モデル(User, Account, Session, VerificationToken, Subscription, SubscriptionTrash, Project, ProjectMessage, ProjectPicture)。
- マイグレーション16件、継続運用中(`prisma/migrations/`)。
- Projectドメインのビジネスロジック・DBアクセスは `src/lib/services/project-service.ts` に集約済み(直近のリファクタで完了)。API routeは薄いコントローラ化されている。
- `src/app/api/account/plan/route.ts`、`src/app/api/cron/cleanup-expired-projects/route.ts` は未分離の可能性があり、着手時に個別確認が必要。

### AWS関連
- `terraform/main.tf`: S3バケット(`project_pictures`)関連のみ定義(バージョニング、公開読み取り、CORS、31日ライフサイクル削除、IAM)。Cognito/Lambda/SESなどは無し。
- コードから直接AWS SDKを呼ぶのは `src/lib/s3.ts` のみ(`@aws-sdk/client-s3`)。
- 画像アップロード機能(`ProjectPicture`)は現状**一時停止中**(`src/app/api/projects/upload/route.ts`が常に400を返す実装になっている)。

### API構成
- `src/app/api` 配下は実質**4エンドポイント**: `/api/account/plan`, `/api/auth/[...nextauth]`, `/api/cron/cleanup-expired-projects`, `/api/projects`(+`/upload`)。分離対象の規模は小さい。

### デプロイ/インフラ
- `Dockerfile`: マルチステージビルド。起動時に`prisma migrate deploy`を実行してから`next start`。
- `docker-compose.yml`/`docker-compose.dev.yml`: Postgres + Next.jsアプリの2サービス構成。
- `vercel.json`: Vercelデプロイ設定もあり、Vercel/Docker(EC2等)のハイブリッド運用の形跡がある(`schema.prisma`のコメントに「EC2側でgenerate」との記載)。

### 主要依存
- Next.js 14.2.16, React 18, TypeScript 5, Prisma 6.14, next-auth v5 beta, `@aws-sdk/client-s3`のみ。
- 状態管理・バリデーションライブラリ(Redux, Zod等)は無く、非常にミニマルな構成。

## 見つかった問題点と対応

調査中に、移行計画とは別に対応が必要な問題を2件発見した。

### 1. `src/lib/s3.ts` の不審なデバッグコード(対応済み)

`getS3Client()` 内に、AWS環境変数(アクセスキーの有無・長さなど)を `http://127.0.0.1:7243/ingest/...` へPOSTするデバッグ用と思われるコードが埋め込まれていた。心当たりがないとのことだったため、**その場で削除した**。

- 修正箇所: `src/lib/s3.ts`(該当のfetch呼び出し2箇所を削除)
- 同様のパターンが `src/app/api/projects/upload/route.ts` にもコメントアウト状態で残っていたため、あわせて削除。
- リポジトリ全体を`grep`し、他に同様のコードが残っていないことを確認済み(`.next`ビルド成果物内にのみ残存。次回ビルドで再生成されるため対応不要)。

### 2. `src/middleware.ts` の認証チェック無効化(持ち越し)

Vercelのmiddleware 1MB制限を理由に、`/dashboard`, `/create`, `/account` 配下のセッション検証(`auth()`呼び出し)が丸ごとコメントアウトされている。API側の`requireAuth()`は機能しているとみられるが、**ページ単位のガードは現状かかっていない**。

- ユーザー判断: 「今は保留、後で判断」
- **持ち越し事項**として記録。ステップ4(認証・認可の切り分け)で、Spring Boot分離後の認証方式とあわせて解消方針を決定する。

## 懸念点・持ち越し事項

- middleware認証チェック無効化の対応方針(上記)。ステップ4に引き継ぐ。
- `src/app/api/account/plan/route.ts`、`src/app/api/cron/cleanup-expired-projects/route.ts` のservice層分離状況の個別確認。ステップ5・7の着手時に確認する。
- `Zaoral_backend3`(公開ページ表示用の別Next.jsサービス、Vercelホスティング)は今回のスコープ外だが、同一DBを直接参照している別サービスが存在するという事実は認識しておく(DB分離を検討する際に影響し得る)。
